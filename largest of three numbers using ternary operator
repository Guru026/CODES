#include <stdio.h>

int main()
{
    int a,b,c;
    printf("enter all three numbers");
    scanf("%d %d %d",&a,&b,&c);
    (a>b)?((a>c)?printf("a is the largest no.",a):printf("c is the largest no.",c)):((b>c)?printf("b is the largest no.",b):printf("c is the largest no.",c));
    return 0;
}
